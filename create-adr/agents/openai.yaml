interface:
  display_name: "Create ADR"
  short_description: "Compatibility entrypoint for the domain-modeling ADR workflow"
  default_prompt: "Use $domain-modeling to create or update an ADR, preserving the repository format and obtaining owner approval when required."
