---
name: create-adr
description: Compatibility entrypoint for creating or updating an Architecture Decision Record. Use when explicitly invoked; the canonical ADR workflow lives in domain-modeling.
---

# Create ADR Compatibility Entry Point

Use the canonical [`domain-modeling`](../domain-modeling/SKILL.md) skill and follow its ADR workflow and [`ADR-FORMAT.md`](../domain-modeling/ADR-FORMAT.md).

This compatibility skill adds no separate rules. Do not implement the recorded decision, push a branch, or deploy without a separate request.
